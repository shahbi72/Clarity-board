name: CI

on:
  push:
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.x

      - name: Enable Corepack and activate pnpm 9.15.9
        run: |
          corepack enable
          corepack prepare pnpm@9.15.9 --activate
          pnpm --version

      - name: Resolve pnpm store path
        id: pnpm-store
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Restore pnpm store cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint (only if script exists)
        run: |
          node -e "const s=require('./package.json').scripts||{}; process.exit(s.lint?0:1)" \
            && pnpm -s run lint \
            || echo "No lint script; skipping"

      - name: Build app
        run: pnpm build

      - name: Install Playwright browser
        run: pnpm -s exec playwright install --with-deps chromium || true

      - name: Run unit tests (only if script exists)
        run: |
          node -e "const s=require('./package.json').scripts||{}; process.exit(s.test?0:1)" \
            && pnpm -s run test \
            || echo "No test script; skipping"

      - name: Run e2e tests (only if script exists)
        run: |
          node -e "const s=require('./package.json').scripts||{}; process.exit(s['test:e2e']?0:1)" \
            && pnpm -s run test:e2e \
            || echo "No test:e2e script; skipping"

      - name: Upload Playwright artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts
          path: |
            test-results
            playwright-report
          if-no-files-found: ignore
